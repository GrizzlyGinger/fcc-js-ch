//Each item in an array has an index
// doubles as the position of the item and how it is referenced
//First position in an array is at the zeroth index
//Syntax: [index]
//can also set the value at an index using tha assignment operator

let myArray = ["a", "b", "c", "d"];
// Only change code below this line
myArray[1] = 'butts';
// Only change code above this line
console.log(myArray);