//More useful to generate random whold numbers
//Need to use Math.floor() to round down to the nearest whole number

function randomWholeNum() {

  // Only change code below this line
  
  return Math.floor(Math.random() * 10);
}