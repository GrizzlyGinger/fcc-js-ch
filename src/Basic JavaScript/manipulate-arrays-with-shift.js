//shift() works like pop(), just with the first value in an array instead of the last
//Syntax: someValue = anArray.shift();

// Setup
var myArray = [["John", 23], ["dog", 3]];

// Only change code below this line
var removedFromMyArray = myArray.shift();