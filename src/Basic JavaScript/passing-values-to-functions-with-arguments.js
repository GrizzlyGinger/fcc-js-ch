//Parameters act as placeholders for values to be put into functions when they are called
//When values are passed to a function they are known as arguments

//Declaring function with parameters
function functionWithArgs(param1, param2) {
  let sum = param1 + param2;
  console.log(sum);
}

//Calling function with two numbers
functionWithArgs(2, 2);