//unshift() works like push(), but adds elements to the beginning of the array instead of the end
//Syntax: anArray.unshift(parameter);

// Setup
var myArray = [["John", 23], ["dog", 3]];
myArray.shift();

// Only change code below this line

myArray.unshift(["Paul", 35]);