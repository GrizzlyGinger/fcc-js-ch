//JS has Math.random() to generate a random decimal between 0(inclusive) and 1(exclusive)

function randomFraction() {

  // Only change code below this line

  return Math.random();

  // Only change code above this line
}