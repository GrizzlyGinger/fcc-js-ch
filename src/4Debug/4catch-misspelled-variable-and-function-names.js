//Transposed, missing, or mis-capitalized characters (spelling errors) are common bugs
// directs browser to search for objects that don't exist
//  reference error

let receivables = 10;
let payables = 8;
// let netWorkingCapital = recievables - payable;
// console.log(`Net working capital is: ${netWorkingCapital}`);

let netWorkingCapital = receivables - payables;
console.log(`Net working capital is: ${netWorkingCapital}`);